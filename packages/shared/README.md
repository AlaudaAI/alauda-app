# @alauda/shared

Shared helpers used by both `apps/base` and `apps/sub-demo`. Demo placeholder
for the future business-logic / UI packages described in
[`AlaudaAI/contexts` design-docs/local-map-seo-modularization.md](https://github.com/AlaudaAI/contexts/blob/main/design-docs/local-map-seo-modularization.md).

Current contents: one `greet(name)` helper. Production `@alauda/business` and
`@alauda/ui` will replace this once the modularization plan lifts off Backlog.
